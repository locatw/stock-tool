# Markdown Coding Guidelines

## Fenced Code Blocks

Always specify a language for fenced code blocks:

````markdown
<!-- Good -->
```go
fmt.Println("hello")
```

<!-- Bad -->
```
fmt.Println("hello")
```
````

Use `text` for non-code blocks (e.g., directory trees).
